> **SAMPLE REPORT.** Committed so the report format is visible in the repo.
> Generated by `python scripts/check_cantor8.py --write-report` against Cantor8 DevNet
> on 2026-08-29 with **no C8_CLIENT_SECRET set**, so the authenticated checks show
> FAIL/SKIPPED and only the public (no-auth) endpoints were exercised. Every value
> here is real, non-sensitive public network data. Re-run with a secret to see the
> ledger checks pass.

# Cantor8 connectivity check

- Generated: `2026-08-29T14:02:19Z`
- Mode: DevNet / Keycloak
- Ledger base: `https://api.validator.dev.digik.cantor8.tech/api/ledger`

**Passed: 4   Failed: 1   Skipped: 4   Manual action required: 5**

## Checks

| Service | Method | Endpoint | Auth | Status | Outcome | Demo | Latency | Result |
|---|---|---|---|---|---|---|---|---|
| Auth | POST | `/realms/master/protocol/openid-connect/token` | yes | - | FAIL | required | - | C8_CLIENT_SECRET is not set. Ask the Cantor8 team for the secret and set it in your shell for this session only (PowerShell: $env:C8_CLIENT_SECRET = "***redacted***"). |
| Ledger | GET | `/v2/state/ledger-end` | yes | - | SKIPPED | required | - | Skipped: no token. |
| Ledger | GET | `/v2/parties` | yes | - | SKIPPED | required | - | Skipped: no token. |
| Ledger | POST | `/v2/state/active-contracts` | yes | - | SKIPPED | required | - | Skipped: no token. |
| Registry | GET | `/registry/metadata/v1/info` | no | 200 | PASS | useful | 421.4 ms | Registry info readable (admin=DSO::1220be58c29e65de40bf273be1dc2b266d43a9a002ea5b18955aeef7aac881bb471a). |
| Registry | GET | `/registry/metadata/v1/instruments` | no | 200 | PASS | useful | 429.2 ms | 1 instrument(s): Amulet(admin=None...,dec=10) |
| Scanner | GET | `/health` | no | 200 | PASS | useful | 74.5 ms | status=ok, db=ok, scannerDelaySecs=0.66218. |
| Scanner | GET | `/tokens/balance/{party}` | yes | - | SKIPPED | optional | - | Skipped: no token. |
| Public Scan | GET | `/api/scan/v0/splice-instance-names` | no | 200 | PASS | optional | 405.6 ms | Public network info readable ({'network_name': 'Canton Network', 'network_favicon_url': 'h). |
| Ledger | POST | `/v2/commands/submit-and-wait` | yes | - | EXPECTED MANUAL ACTION | optional | - | Not run by the diagnostic - write/mutating action. |
| Ledger | POST | `/v2/parties` | yes | - | EXPECTED MANUAL ACTION | optional | - | Not run by the diagnostic - write/mutating action. |
| Ledger | POST | `/v2/users/{userId}/rights` | yes | - | EXPECTED MANUAL ACTION | optional | - | Not run by the diagnostic - write/mutating action. |
| Registry | POST | `/registry/transfer-instruction/v1/transfer-factory (exercise)` | yes | - | EXPECTED MANUAL ACTION | optional | - | Not run by the diagnostic - write/mutating action. |
| Registry | POST | `/registry/transfer-instruction/v1/{id}/choice-contexts/accept` | yes | - | EXPECTED MANUAL ACTION | optional | - | Not run by the diagnostic - write/mutating action. |

## What each result means

- **Auth POST /realms/master/protocol/openid-connect/token** (FAIL): No client secret is set, so no token can be requested; every authenticated check below is skipped.
- **Ledger GET /v2/state/ledger-end** (SKIPPED): Needs a token, which was not available.
- **Ledger GET /v2/parties** (SKIPPED): Needs a token, which was not available.
- **Ledger POST /v2/state/active-contracts** (SKIPPED): Needs a token, which was not available.
- **Registry GET /registry/metadata/v1/info** (PASS): The registry advertises its admin party and supported transfer features; it is public (no token).
- **Registry GET /registry/metadata/v1/instruments** (PASS): Lists every token this registry serves with its admin and decimals - stops Scandex assuming every asset is Amulet.
- **Scanner GET /health** (PASS): The scanner is an INDEXED read model and can lag; scannerDelaySecs says by how much.
- **Scanner GET /tokens/balance/{party}** (SKIPPED): This endpoint needs a token; with one, a 401 would mean 'who are you' and a 403 'not yours / m2m only'.
- **Public Scan GET /api/scan/v0/splice-instance-names** (PASS): Network-wide branding published by super validators; no token needed.
- **Ledger POST /v2/commands/submit-and-wait** (EXPECTED MANUAL ACTION): Submitting a command (a transfer, an accept, any write) moves real value. Needs a separate, explicitly-invoked command with human approval; never runs in CI.
- **Ledger POST /v2/parties** (EXPECTED MANUAL ACTION): Allocating a party changes ledger topology. Needs a separate, explicitly-invoked command with human approval; never runs in CI.
- **Ledger POST /v2/users/{userId}/rights** (EXPECTED MANUAL ACTION): Granting act-as rights changes who can spend a party's assets. Needs a separate, explicitly-invoked command with human approval; never runs in CI.
- **Registry POST /registry/transfer-instruction/v1/transfer-factory (exercise)** (EXPECTED MANUAL ACTION): Exercising the returned factory executes the transfer. Preview only here. Needs a separate, explicitly-invoked command with human approval; never runs in CI.
- **Registry POST /registry/transfer-instruction/v1/{id}/choice-contexts/accept** (EXPECTED MANUAL ACTION): Accepting an offer moves value to the receiver. Needs a separate, explicitly-invoked command with human approval; never runs in CI.

## Not tested

This run never exercises a write or streaming endpoint. The system is therefore **not** fully tested:

- `POST /v2/commands/submit-and-wait (write)`
- `WS /v2/updates (streaming)`
